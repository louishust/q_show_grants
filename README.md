The q_show_grants is used to show user's privileges, The usage like below:
q_show_grants --host=xxx --port=xxx --user=xxx --pass=xxx

## How to compile from source
   1. you have the get go installed
   2. set GOROOT and GOPATH enviroment variables
   3. make setup

## How to clean
   make clean
